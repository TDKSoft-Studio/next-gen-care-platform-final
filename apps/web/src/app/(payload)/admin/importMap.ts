import type { ImportMap } from "payload";

// No custom Admin components are registered during the technical spike.
// Payload regenerates this map when component registrations are introduced.
export const importMap: ImportMap = {};

# Required at runtime. Never commit real values.
DATABASE_URL=postgresql://payload:replace-me@127.0.0.1:5432/next_gen_care
PAYLOAD_SECRET=replace-with-at-least-32-random-characters

# Approved public origin. Change only through reviewed environment configuration.
NEXT_PUBLIC_SITE_URL=https://www.nextgen-cares.org

# Search indexing remains disabled until content, legal, accessibility, and release gates are approved.
SITE_INDEXING_ENABLED=false

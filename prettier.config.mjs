/** @type {import("prettier").Config} */
const config = {
  arrowParens: "always",
  printWidth: 100,
  proseWrap: "preserve",
  semi: true,
  singleQuote: false,
  trailingComma: "none"
};

export default config;
